package shapes;
//Inside of the shapes directory, create a Measurable interface.
public interface Measurable {
    //Create an abstract Shape class inside of the shapes directory.
    double getPerimeter();
    double getArea();
}